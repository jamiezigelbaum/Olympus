{
  "name": "olympus-google-exchange",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker: publisher-side Google OAuth token exchange for Olympus. See docs/ops/GOOGLE_EXCHANGE_ENDPOINT.md.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
